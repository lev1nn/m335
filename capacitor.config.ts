import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.m335',
  appName: 'm335',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
